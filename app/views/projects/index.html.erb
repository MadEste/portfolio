<h1>This is Placeholder for Projects</h1>

<% @projects.each_slice(3) do |projects| %>
	<div class="row">
		<% projects.each do |project| %>
			<div class="col-md-4">
				<div class="recipe">
					<div class="image_wrapper">
						<%= link_to project do %>
							<%= image_tag project.url, style: "width:200px;height:200px;" %>
						<% end %>
					</div>
					<h2><%= link_to project.title, project %></h2>
					<% if admin_signed_in? %>
						<%= link_to "Edit", edit_project_path(project) %>
					<% end %>
				</div>
			</div>
		<% end %>
	</div>		
<% end %>
<div class="row">
	<% if admin_signed_in? %>
		<%= link_to "New", new_project_path %> 
	<% end %>
</div>