This Portfolo Site App is meant to be deployed as a 1 user system.

Devise Registrations have been disabled

After deploying add addmin via rails console. 

category models must also be added via rails console. 

Gems Used ==>  gem 'bootstrap-sass', '~> 3.3.5.1' 	(easy bootstrap implementation)
					gem 'simple_form', '~> 3.1.0'			(Forms)
					gem 'devise', '~> 3.5.1'				(Admin authentication)
					gem 'friendly_id', '~> 5.1.0'			(custom slugs)
